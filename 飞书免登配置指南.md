# 飞书免登功能 - 配置与使用指南

## 一、功能概述

本次改造为社团管理系统增加了**飞书免登**功能：
- 老师在飞书客户端内打开应用 → 自动识别身份并登录，无需输入密码/PIN
- 外部链接打开 → 保持原有的密码/PIN登录方式，完全不受影响
- 两种登录方式并存，校内用飞书，校外用链接

## 二、飞书开放平台配置（管理员操作）

### 步骤1：创建企业自建应用

1. 登录 [飞书开放平台](https://open.feishu.cn/)
2. 点击「创建企业自建应用」
3. 填写应用名称（如「社团考勤管理」），上传图标，点击创建

### 步骤2：获取 App ID 和 App Secret

1. 进入应用 → 「凭证与基础信息」
2. 复制 **App ID** 和 **App Secret**（后续配置到部署环境）

### 步骤3：添加网页应用能力

1. 左侧菜单 → 「应用能力」→ 「网页」
2. 开启「桌面端」和「移动端」
3. 桌面端主页地址：填写你的部署地址（如 `https://your-app.up.railway.app`）
4. 移动端主页地址：同上

### 步骤4：配置重定向URL（重要！）

1. 左侧菜单 → 「安全设置」
2. 找到「重定向URL」，点击添加
3. 填写你的部署地址根路径（必须与应用主页一致），例如：
   ```
   https://your-app.up.railway.app/
   ```
4. 注意：必须包含 `https://`，末尾的 `/` 建议加上

### 步骤5：申请权限

1. 左侧菜单 → 「权限管理」
2. 搜索并申请以下权限：
   - `contact:user.base:readonly` — 获取用户基本信息（姓名等）
   - `contact:user.phone:readonly` — 获取用户手机号（**必须申请，否则免登无法按手机号匹配**）
3. 点击「申请权限」，等待企业管理员审批

> 注意：手机号权限可能需要企业管理员在飞书管理后台审批。如果暂时无法获取手机号权限，系统会自动降级为按姓名匹配（社团老师姓名与飞书姓名一致即可免登）。

### 步骤6：创建版本并发布

1. 左侧菜单 → 「版本管理与发布」
2. 点击「创建版本」，填写版本号和更新说明
3. 设置「可用范围」（建议选择「全部成员」或指定部门）
4. 点击「申请发布」，等待企业管理员审核通过

## 三、部署环境配置

在你的部署平台（Railway / Render / 其他）中，添加以下环境变量：

| 环境变量 | 值 | 说明 |
|---------|-----|------|
| `FEISHU_APP_ID` | 飞书应用的 App ID | 从飞书开放平台获取 |
| `FEISHU_APP_SECRET` | 飞书应用的 App Secret | 从飞书开放平台获取 |

配置完成后重新部署，系统会自动检测到这两个变量并启用飞书免登。

> 验证方法：访问 `https://你的地址/api/feishu/config`，应返回 `{"enabled":true,"appId":"cli_xxx"}`

## 四、系统内绑定手机号（管理员操作）

### 1. 绑定管理员手机号

1. 用管理员密码登录系统
2. 进入「系统设置」标签页
3. 在「飞书免登设置」中输入管理员的飞书手机号
4. 点击「保存手机号」

### 2. 绑定社团老师手机号

方式一（管理员操作）：
1. 管理员登录 → 点击社团进入详情页
2. 点击「编辑」按钮
3. 在「老师手机号」一栏输入该社团指导老师的飞书手机号
4. 点击「保存」

方式二（老师自己操作）：
1. 老师用 PIN 码登录自己的社团
2. 进入社团详情页 → 点击「编辑」
3. 输入自己的飞书手机号 → 保存

## 五、身份匹配规则

飞书用户打开应用时，系统按以下优先级匹配身份：

1. **管理员匹配**：飞书手机号 == 系统设置的管理员手机号 → 自动登录管理员
2. **社团老师匹配（手机号）**：飞书手机号 == 某社团设置的老师手机号 → 自动登录该社团
3. **社团老师匹配（姓名）**：飞书姓名 == 某社团的指导老师姓名（且该社团未设置手机号）→ 自动登录该社团
4. **都匹配不到**：提示「您还未绑定社团，请联系管理员」，但仍可手动输入 PIN 码登录

## 六、使用流程

### 老师使用飞书免登
1. 打开飞书 → 工作台 → 找到「社团考勤管理」应用
2. 点击打开 → 系统自动识别身份并登录
3. 首次使用如果提示未绑定，请联系管理员在系统中设置你的手机号

### 外部链接登录（不变）
- 管理员：输入管理员密码登录
- 社团老师：选择社团 → 输入 PIN 码登录

## 七、常见问题

**Q: 飞书内打开应用还是需要登录？**
A: 检查以下几点：
1. 部署环境是否配置了 FEISHU_APP_ID 和 FEISHU_APP_SECRET
2. 飞书开放平台的重定向URL是否正确配置
3. 该用户的手机号是否已在系统中绑定
4. 访问 /api/feishu/config 确认 enabled 为 true

**Q: 提示「您还未绑定社团」怎么办？**
A: 联系管理员，在系统中为你设置飞书手机号（社团详情页编辑 → 老师手机号）

**Q: 手机号权限申请不下来怎么办？**
A: 系统会自动降级为姓名匹配。确保社团的「指导老师」姓名与飞书显示姓名完全一致即可。

**Q: 外部链接用户会受影响吗？**
A: 完全不受影响。飞书免登只在飞书客户端内触发，外部浏览器打开仍走原有的密码/PIN登录。

**Q: Node.js 版本有要求吗？**
A: 需要 Node.js 18+（因为使用了全局 fetch API）。Railway 默认版本满足要求。

## 八、修改的文件清单

| 文件 | 修改内容 |
|------|---------|
| `server.js` | 新增飞书配置、数据升级、免登接口、手机号字段支持 |
| `public/index.html` | 社团编辑增加手机号输入框、管理员设置增加飞书免登设置 |
| `public/js/app.js` | 飞书免登全套前端逻辑、手机号保存与显示 |

## 九、回退方案

如果飞书免登出现问题，只需在部署环境中删除 `FEISHU_APP_ID` 和 `FEISHU_APP_SECRET` 两个环境变量，重新部署即可关闭飞书免登，系统自动回退到纯密码/PIN登录模式。
