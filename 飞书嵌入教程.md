# 清华附中社团管理系统 — 飞书嵌入教程

把已部署在 Railway 上的社团管理系统嵌入到飞书工作台，师生在飞书里点一下图标就能直接打开，无需记网址。

---

## 前提条件

- 系统已部署在 Railway 上，有一个公网访问地址（形如 `https://xxx.up.railway.app`）
- 你是学校的飞书管理员，或者能联系到飞书管理员帮忙审核应用

> 如果你只是普通飞书用户（非管理员），也可以创建应用，但发布上线需要管理员审核。建议直接和学校飞书管理员一起操作。

---

## 第一步：创建飞书自建应用

1. 打开 **飞书开放平台**：https://open.feishu.cn/app/
2. 用你的飞书账号登录
3. 点击右上角 **「创建企业自建应用」**
4. 填写应用信息：
   - **应用名称**：`清华附中社团管理`
   - **应用描述**：`初中社团管理系统`
   - **应用图标**：上传校徽或任意图标（建议 512×512px）
5. 点击 **「创建」**

---

## 第二步：添加「网页应用」能力

1. 进入刚创建的应用详情页
2. 左侧菜单点击 **「应用能力」→「添加应用能力」**
3. 找到 **「网页应用」** 卡片，点击 **「添加」**
4. 添加后，左侧菜单会出现 **「网页应用」** 子菜单，点进去

---

## 第三步：配置网页主页地址

在「网页应用」配置页面，填写：

| 字段 | 填什么 |
|------|--------|
| **桌面端主页地址** | 你的 Railway 部署地址，例如 `https://xxx.up.railway.app` |
| **移动端主页地址** | 同上（手机飞书也会用这个地址打开） |
| **打开方式** | 选「在飞书内打开」 |

> 把 `xxx` 替换成你在 Railway 上的实际域名。打开 Railway 控制台 → 你的项目 → Settings → Networking，能看到公网域名。

填好后点击底部 **「保存」**。

---

## 第四步：创建版本并发布

1. 左侧菜单点击 **「版本管理与发布」**
2. 点击右上角 **「创建版本」**
3. 填写：
   - **应用版本号**：`1.0.0`
   - **更新说明**：`社团管理系统首次上线`
4. **可用范围**：选择 **「全部员工」** 或指定部门（如初中部全体师生）
   - 这里选的人，就是在飞书工作台能看到这个应用的人
5. 点击 **「保存」→「申请发布」**

---

## 第五步：管理员审核

1. 学校的飞书管理员会收到审核通知
2. 管理员登录 **飞书管理后台**（https://feishu.cn/admin）
3. 进入 **「工作台」→「应用管理」**，找到待审核的应用
4. 点击 **「审核通过」**

> 如果你自己就是飞书管理员，可以直接切到管理后台审核。

---

## 第六步：开始使用

审核通过后：

- **飞书电脑端**：工作台（左下角九宫格图标）里会出现「清华附中社团管理」图标，点击即打开
- **飞书手机端**：工作台 → 找到应用图标 → 点击打开
- 打开后还是需要登录（管理员用密码，教师用PIN码），和现在完全一样

---

## 高级功能（可选）：飞书免登

当前嵌入后，师生打开应用仍需输入密码/PIN。如果想做到**在飞书里打开自动登录、不用再输密码**，需要对接飞书的「免登」能力：

### 原理
飞书打开网页应用时，会在 URL 上附带一个临时授权码（`code`），后端拿这个 code 去飞书换取用户身份（姓名、手机号等），自动登录。

### 需要改的内容
1. **飞书侧配置**：在应用详情 →「安全设置」→ 添加重定向 URL（你的 Railway 地址）
2. **后端 server.js**：新增 `/api/feishu/auth` 接口，用 App ID + App Secret 向飞书换取 `user_access_token`，再获取用户信息
3. **前端 app.js**：检测 URL 上的飞书 `code` 参数，自动调用免登接口
4. **权限管理**：在「权限管理」中开通「获取用户基本信息」等接口权限

### 工作量评估
免登功能涉及飞书 OAuth 对接，需要改动后端和前端代码，**预计 1-2 小时开发 + 调试**。

> 如果学校师生人数不多、大家对输入密码/PIN 没有意见，**可以先不做免登**，直接用最简嵌入方式即可。后续需要时再告诉我，我帮你加。

---

## 常见问题

**Q：嵌入后页面空白 / 打不开？**
A：检查 Railway 地址是否正常访问（浏览器直接打开试试），确认地址以 `https://` 开头。

**Q：手机飞书能打开但电脑端打不开？**
A：确认「桌面端主页地址」也填了同一个 URL。

**Q：只有部分人能看到应用？**
A：检查发布时的「可用范围」设置，以及管理员审核时是否限制了可见部门。

**Q：系统更新了，飞书里需要重新配置吗？**
A：不需要。Railway 重新部署后地址不变，飞书里自动就是最新版本。

**Q：应用图标在哪里设置？**
A：应用详情 →「基础信息」→ 上传应用图标。

---

## 操作总结

| 步骤 | 在哪里做 | 做什么 |
|------|----------|--------|
| ① | 飞书开放平台 | 创建自建应用 |
| ② | 应用详情页 | 添加网页应用能力 |
| ③ | 网页应用配置 | 填入 Railway 公网地址 |
| ④ | 版本管理与发布 | 创建版本 + 选可见范围 |
| ⑤ | 飞书管理后台 | 管理员审核通过 |
| ⑥ | 飞书工作台 | 师生点击图标使用 |

整个过程不改动任何代码，纯配置，**10 分钟即可完成**。
