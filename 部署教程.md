# Railway 部署手把手教程

> 适合完全没有部署经验的人，每一步都有截图说明位置

---

## 前提条件

你的电脑上已经有这些文件（位于 `C:\Users\HW\WorkBuddy\2026-07-31-20-02-54\club-management\`）：
- server.js
- package.json
- Procfile
- .gitignore
- data.json
- README.md
- public/ 文件夹（含 index.html, css/, js/, images/）

---

## 第一步：把代码推送到 GitHub

你的 GitHub 仓库已经创建好了：`https://github.com/FS1158/club-management`

### 方法A：用命令行推送（推荐）

1. **打开 Windows 命令行**
   - 按 `Win + R`，输入 `cmd`，回车

2. **进入项目目录**
   ```
   cd C:\Users\HW\WorkBuddy\2026-07-31-20-02-54\club-management
   ```

3. **执行以下命令依次推送**
   ```
   git add .
   git commit -m "社团考勤系统"
   git branch -M main
   git remote add origin https://github.com/FS1158/club-management.git
   git push -u origin main
   ```

4. **如果提示输入用户名和密码**
   - 用户名：你的 GitHub 用户名 `FS1158`
   - 密码：**不是你的 GitHub 登录密码**，需要用 **Personal Access Token（PAT）**
   - 获取 PAT 的方法：
     1. 打开 https://github.com/settings/tokens
     2. 点击 **"Generate new token" → "Generate new token (classic)"**
     3. 填写 Note（随便写，如 "deploy"）
     4. Expiration 选 90 days
     5. 勾选 **repo**（全部）
     6. 点击底部绿色按钮 "Generate token"
     7. **复制生成的 token**（只显示一次！）
     8. 推送时密码栏粘贴这个 token

5. **推送成功后**
   - 打开 https://github.com/FS1158/club-management
   - 应该能看到所有文件

### 方法B：用 GitHub Desktop 推送（更简单）

1. 下载安装 GitHub Desktop：https://desktop.github.com/
2. 打开后登录你的 GitHub 账号
3. 点击 "Add an Existing Repository from your hard drive"
4. 选择文件夹 `C:\Users\HW\WorkBuddy\2026-07-31-20-02-54\club-management`
5. 在左下角填写描述，点 "Commit to main"
6. 点击上方 "Push origin" 推送到 GitHub

### 方法C：网页直接上传（最简单但慢）

1. 打开 https://github.com/FS1158/club-management
2. 点击 "uploading an existing file" 链接
3. 把以下文件/文件夹拖进去：
   - server.js
   - package.json
   - Procfile
   - .gitignore
   - data.json
   - README.md
   - public 文件夹（需要先压缩成 zip，GitHub 不支持直接上传文件夹）

   **注意**：如果用网页上传，建议先把这些文件用压缩工具打包，再解压到仓库。最简单的方式还是用方法A或B。

---

## 第二步：在 Railway 部署

### 2.1 注册 Railway

1. 打开 https://railway.app
2. 点击右上角 **"Login"**
3. 选择 **"Login with GitHub"**（用 GitHub 账号登录）
4. 授权 Railway 访问你的 GitHub

### 2.2 创建新项目

5. 登录后，点击 **"New Project"**（新项目）
6. 选择 **"Deploy from GitHub repo"**（从 GitHub 仓库部署）
7. 如果第一次使用，会提示你授权 Railway 访问 GitHub 仓库
   - 点击 **"Configure GitHub"**
   - 选择你的 GitHub 账号
   - 在权限页面选择 **"Only select repositories"**
   - 勾选 `club-management` 仓库
   - 点击 **"Install"**
8. 回到 Railway 后，在仓库列表中选择 **`club-management`**

### 2.3 配置部署

9. Railway 会自动检测到这是一个 Node.js 项目
10. **检查设置**（通常不需要改，但确认一下）：
    - Build Command 应该自动识别为 `npm install`
    - Start Command 应该自动识别为 `node server.js`
    - 如果没有自动识别，手动设置：
      - 点项目页面 → **Settings** 标签
      - Build Command: `npm install`
      - Start Command: `node server.js`

### 2.4 设置端口

11. Railway 会自动注入 `PORT` 环境变量，你的 server.js 已经支持了
12. **不需要额外设置端口**（Railway 自动处理）

### 2.5 等待部署

13. Railway 会自动开始构建和部署
14. 在 **"Deployments"** 标签可以看到构建日志
15. 等待出现绿色的 **"Active"** 状态（通常 1-2 分钟）

### 2.6 获取公网访问地址

16. 部署成功后，在项目页面找到 **"Settings"** 标签
17. 找到 **"Networking"** 部分
18. 点击 **"Generate Domain"**（生成域名）
19. 你会得到一个地址，类似：
    ```
    https://club-management-production.up.railway.app
    ```

20. **打开这个地址**，应该能看到社团考勤系统的登录页面！

---

## 第三步：验证和分享

### 3.1 验证功能
1. 用管理员密码 `admin123` 登录，确认能看到仪表盘
2. 用社团 PIN 码 `1234` 登录篮球社，确认能编辑
3. 测试考勤打卡功能

### 3.2 分享给老师
1. 把 Railway 给你的公网地址发给所有社团老师
2. 告诉每个老师：
   - 打开网址
   - 选择"教师登录"
   - 选自己的社团
   - 输入4位PIN码

### 3.3 添加到手机桌面
1. 用手机浏览器打开网址
2. iPhone Safari：点击底部"分享"→"添加到主屏幕"
3. Android Chrome：点击右上角菜单→"添加到主屏幕"
4. 桌面会出现APP图标，直接点开就能用

---

## 常见问题排查

### Q: 推送代码时报错 "Authentication failed"
- 原因：GitHub 不再支持密码认证
- 解决：使用 Personal Access Token（PAT），方法见上面第一步方法A

### Q: Railway 构建失败
1. 检查 GitHub 仓库里是否有 `package.json`（必须有）
2. 检查 `package.json` 里有 `"start": "node server.js"` 脚本
3. 在 Railway 的 Deployments 标签查看错误日志

### Q: Railway 部署成功但打开是白屏
1. 检查 Railway 日志（Deployments → 点最新部署 → View Logs）
2. 确认没有 "Cannot find module" 错误
3. 确认 `public` 文件夹下的文件都已上传到 GitHub

### Q: 打开后报错 "Application error"
- 在 Railway 项目页面点 **"Logs"** 标签查看运行日志
- 常见原因：data.json 权限问题或路径问题

### Q: 部署后数据丢失
- Railway 的文件系统是**临时的**，每次重新部署 data.json 会重置
- 如果需要持久化数据，需要在 Railway 添加 Volume：
  1. 项目页面 → Settings → Volumes
  2. 添加 Volume，挂载路径设为 `/app`（或对应路径）
  3. 这样 data.json 就会持久保存

---

## 部署后的管理

### 修改管理员密码
1. 登录管理员账号
2. 进入设置页面修改密码
3. 注意：如果重新部署，密码会重置为 admin123

### 更新代码
1. 修改本地文件后
2. 执行 `git add .` → `git commit -m "更新说明"` → `git push`
3. Railway 会自动检测到更新并重新部署

### 查看使用量
1. Railway 控制台首页可以看到资源使用量
2. 免费额度包含：500小时/月执行时间、$5 信用额度
3. 这个应用很轻量，免费额度完全够用
